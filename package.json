{
  "name": "ci-project",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "build": "echo Build Successful",
    "package": "tar -czf app.tar.gz *"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
